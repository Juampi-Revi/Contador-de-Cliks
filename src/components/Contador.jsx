import '../stylesheet/Contador.css';

function Contador ({ numContador }) {
    return (
        <div className="Contador">
            { numContador }
        </div>
    )
}

export default Contador;